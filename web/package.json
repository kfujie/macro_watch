{
  "name": "macro-watch-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Experimental web view for the macro_watch weekly rates brief.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@observablehq/plot": "^0.6.16"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
